defaults:
    - _self_
    - dataset: default
    - optim: adamw
    - model: dinov2_flanT5
    - loss_fn: msle

epochs: 20
log: True
prefix: ""
experiment_name: ${prefix}${model.name}_${now:%Y-%m-%d_%H-%M-%S}

# Enable this flag for second phase training to reduce "Solid Performers" sampling by half
use_balanced_sampler: False

hydra:
  output_subdir: null
  run:
    dir: .

datamodule:
  _target_: data.datamodule.DataModule
  dataset_path: ${data_dir}
  train_transform: ${dataset.train_transform}
  test_transform: ${dataset.val_transform}
  batch_size: ${dataset.batch_size}
  num_workers: ${dataset.num_workers}
  metadata: ${dataset.metadata}

data_dir: ${root_dir}/dataset/
root_dir: ${hydra:runtime.cwd}
checkpoint_path: ${root_dir}/checkpoints/${experiment_name}_dinov2_flanT5_prompt_resume_2.pt
# checkpoint_to_load: ${root_dir}/checkpoints/dinov2_with_flant5_2025-05-10_21-36-42_dinov2_flanT5_prompt_resume_best.pt



